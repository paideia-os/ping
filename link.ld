/* ping linker script (mirrors tools/user/pdxsock/link.ld and
 * paideia-os src/user/true.ld -- R64v2 satellite-linking closure,
 * paideia-os #1976). ENTRY(_start), .text at 0x00400000, .data at
 * 0x00600000, .bss contiguous with .data (no fixed-address gap
 * between .data and .bss -- tools/verify-user-image-extent.sh in
 * paideia-os fails the build if a PT_LOAD reacquires a hole).
 */

OUTPUT_FORMAT(elf64-x86-64)
OUTPUT_ARCH(i386:x86-64)
ENTRY(_start)

PHDRS
{
    text PT_LOAD FLAGS(5);
    data PT_LOAD FLAGS(6);
}

SECTIONS
{
    . = 0x00400000;

    .text : ALIGN(4K) {
        KEEP(*(.text._start))
        *(.text .text.*)
    } :text

    .rodata : ALIGN(8) {
        *(.rodata .rodata.*)
    } :text

    . = 0x00600000;

    .data : ALIGN(4K) {
        *(.data .data.*)
    } :data

    .bss (NOLOAD) : ALIGN(4K) {
        *(COMMON)
        *(.bss .bss.*)
        . = ALIGN(4K);
    } :data

    /DISCARD/ : {
        *(.comment)
        *(.eh_frame*)
        *(.note.*)
    }
}
